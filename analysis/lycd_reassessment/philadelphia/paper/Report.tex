\documentclass[11pt]{article}
\usepackage[T1]{fontenc}      % proper glyphs (<, >, |) and hyphenation
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}\graphicspath{{figures/}}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{xurl}              % break long URLs at safe characters
\usepackage[htt]{hyphenat}    % allow breaks inside \texttt{} (file paths)
\sloppy                        % prefer loose spacing over right-margin overflow
\emergencystretch=3em          % last-resort stretch for unbreakable lines
\input{ppi-theme.tex}

% Generated headline numbers — produced by paper/scripts/generate_paper_assets.py.
% Run that generator BEFORE writing/compiling prose; never type result numbers here.
\InputIfFileExists{tables/headlines.tex}{}{}
% Fallback so the skeleton compiles before the generator has run:
\providecommand{\LycdHeadline}{\textbf{[run generate\_paper\_assets.py]}}

\begin{document}

\begin{center}
  \IfFileExists{figures/ppi-logo.pdf}{\includegraphics[height=1.3cm]{ppi-logo}\\[0.6em]}{\IfFileExists{figures/ppi-logo.png}{\includegraphics[height=1.3cm]{ppi-logo}\\[0.6em]}{}}{\LARGE\bfseries\color{ppiprimary} Fixing Philadelphia's land assessments without moving anyone's total\par}
  \vspace{0.5em}
  {\large Russell Richie\par}
  \vspace{0.2em}
  {\normalsize Progress and Poverty Institute\par}
  \vspace{0.2em}
  {\normalsize \today\par}
\end{center}
\vspace{1em}



\begin{abstract}
Executive summary: the claim in one or two sentences, the headline numbers (as generated
macros, e.g.\ \LycdHeadline{}), and the policy implication. Write this after the
assets exist and you have read them.
\end{abstract}

\section{Background}
% The policy question and why it matters.

\section{Data and methods}
% Sources (with vintage), how key quantities are computed, main assumptions, precedents.

\section{Results}
% WRITE THIS AFTER generating and inspecting the assets. One sentence per figure/table on
% what it shows. Use macros for every inline number. Example float scaffolding:
%
% \begin{figure}[t]\centering
%   \includegraphics[width=\linewidth]{example.pdf}
%   \caption{What this shows.}\label{fig:example}
% \end{figure}
%
% \begin{table}[t]\centering
%   \caption{What this shows.}\label{tab:example}
%   \input{tables/example.tex}
% \end{table}

\section{Caveats}
% Sample sizes, confidence intervals, robustness, what would change the conclusion.

\section{Policy implication}
% Tie the evidence back to the decision.

\paragraph{Reproducibility.} Every number, table, and figure in this report is generated by
\texttt{paper/scripts/generate\_paper\_assets.py} from the analysis outputs; rebuild with a
single command.

\end{document}
